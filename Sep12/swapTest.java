public void swapTest(int[]a, int pos1, int pos2) {
   if(pos1 >= 0 && pos1 < a.length && pos2>=0&& pos2 < a.length) {
      if(a[pos1] < a[pos2]) {
         int temp = a[pos1];
         a[pos1] = a[pos2];
         a[pos2] ] temp;
      }
   }
}